# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-20 16:01:48
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `skill-options-scanner.md`: Options IQ — Autonomous Scanner (v2.3 — Curated Edge Monitor)
- `skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 17–18, 2026 — Session 28
**Built a standing, self-triggerable hub-wide audit framework — reviewed each engine's own existing audit culture before designing it rather than inventing one from scratch — ran it across all three engines plus the hub's own skills, caught one real drift-since-last-audit finding and one real doc-desync false alarm, and closed by explicitly deciding audits are diminishing returns and pivoting back to forward-test operation.**
- **Before building anything, read what already existed:** discovered all three engines already run their own mature, actively-used audit frameworks with genuinely different taxonomies — Gemini's `AUDIT.md` (3 Pillars, informal labels), OptionsIQ's `MASTER_AUDIT_FRAMEWORK.md` v1.7 (10 categories, VERIFIED/PLAUSIBLE/MISLEADING/BROKEN/FALSE), STA's `MASTER_AUDIT_FRAMEWORK.md` (5 audit types, VERIFIED/PLAUSIBLE/MISLEADING/UNVERIFIED/HALLUCINATED). The hub's own `GOLDEN_RULES.md` only knew about STA's version — a real, still-open finding, logged not silently patched. Also found OptionsIQ has its own two-persona system (Rule 22 — Persona A architect + **Marcus Webb**, a fully-specified 30-year options trader in `QUANT_PERSONA.md`) distinct from the hub's blended Alex, and the canonical `AUDIT_MODE_PROMPT_TEMPLATE.md` (verdict labels, decision tree) Bala had already been using informally.
- **Built `HUB_AUDIT_FRAMEWORK.md`** as a standing, undated reference doc (mirroring the engines' own convention) — full agent prompts baked in, not just descriptions, so a future session can re-trigger any slice with a phrase ("run the hub audit", "— OptionsIQ only", "— STA tabs", "— skills") without re-deriving the design. Self-challenged the first draft twice before finalizing: split a combined Opus persona pass into 3 separate single-persona agents (voice-bleed risk between Marcus Webb and Alex outweighed the saved overhead) and split OptionsIQ's one oversized Fable pass into backend/frontend to avoid the same shallow-read risk already flagged for STA.
- **Ran the 5-agent Fable pass across Gemini/OptionsIQ/STA/hub docs** (~91 findings, 0 CRITICAL, 9 HIGH). Real finding, not just doc lag: Gemini's own July 15 "AUDIT PASSED" claimed `last_gamma` is preserved through DB updates — spot-checked directly and found `PUT /journal/update` actually resets it to 0.0 on every manual edit, a genuine regression since that audit. OptionsIQ (audited for the first time; 8 HIGH total) turned up a dead cache-write path, an unbounded stale-cache fallback, and two frontend bugs showing contradictory risk language on the same trade — all now out of scope per Bala's call mid-session (OptionsIQ dropped entirely going forward), findings preserved in `HANDOFF_optionsiq_audit_session28.md` but not actively tracked.
- **Fixed the hub's own most consequential doc-accuracy bug the same run:** `CLAUDE_CONTEXT.md` and the auto-generated `GEMINI_STATE_HANDOFF.md` both still stated the forward-test target as ×1.80, reverted to ×1.60 back in Session 27 — corrected in both files, regenerated properly.
- **Extended to STA specifically at Bala's direction** — mapped all 8 real UI tabs (Analyze, Scan, Sectors, Context, Value, Validate, DataSources, Forward Testing) directly from source (`grep` on `App.jsx`/`backend.py`, no browser automation needed, resolving "value tabs" as a literal `ValueTab.jsx`) and ran 5 Fable + 2 Opus/Alex agents against them. No fabricated data found on any tab — the repeat pattern instead was code comments being more honest than the UI: the Sector Rotation Monitor's own code admits it's "NOT standard de Kempenaer RRG," but the screen claims "100 = market parity" (false) and a wrong data-source label (says TwelveData, is actually yfinance); the Scan tab's "Minervini" strategy checks only 1-2 of Minervini's real 8 criteria while claiming the full pedigree, worse once Alex traced it — the correct 8-criteria version already exists elsewhere in the same codebase, making it a naming lie, not a capability gap. **Bala's explicit call: don't rename it as a lesser proxy of Minervini either** — STA's own pattern-recognition system (the real 8-criteria Trend Template, VCP, Cup & Handle, all correctly implemented, plus a genuinely backtested `best` strategy) is good in its own right and deserves its own name, not one framed as a downgrade. Full findings + both Opus verdicts in `HANDOFF_sta_audit_session28.md`.
- **Extended again to the hub's own trading-methodology skills** (Scanner, Radar, Directional Builder, Trade Validator — the 4 that actually drive Gemini's trades) — no Fable pre-pass needed, the skills are small enough to read directly. This surfaced the most consequential finding of the whole run: **Directional Builder's "strict majority of up to 6-8 signals" is really trend counted 4-plus ways plus flow once** (SMA200, EMA stack, YTD change, and 52-week range position are all the same underlying signal), manufacturing false confidence exactly in the direction of the prevailing trend — and **MIXED direction isn't a real hard gate**, it just prompts the user to pick a side and proceeds regardless, with Step 6 skippable entirely if a direction is pre-declared. Both logged as open HIGH findings (Known Issues), not yet fixed. Scanner/Radar review found the sieve's one truly mathematically-grounded gate (IV/HV<100%) is real and defensible, but the IVR≤45 threshold's own stated rationale ("above the median") doesn't match its own number (45 ≠ the 50th-percentile median it claims to be).
- **The Opus Scanner/Radar pass also flagged Gate C's units question as CRITICAL** — reading only `OPTIONS_SIEVE_SPEC.md`'s still-open "known unresolved issue" text, not knowing Known Issues already marked it RESOLVED. Checked directly rather than picking a side from the two conflicting docs: pulled live `avg_90d_usd_volume` for NVDA ($31.7B) and HIVE ($78.5M, matching the $83.2M/day already recorded live in Session 27) — confirmed genuinely a daily average, not a 90-day total, settling it with fresh data instead of re-reading old evidence. Fixed the doc desync (`OPTIONS_SIEVE_SPEC.md` now matches Known Issues).
- **Closed the audit thread deliberately, not by running out of things to check:** after the Gate C resolution, Bala pushed back directly — "the more I run audit, the more you keep findings, where do we end... we have to end and keep doing fwd testing and tweak." Agreed explicitly rather than continuing: triaged everything found into "load-bearing for the forward test's own integrity" (just Gate C, now resolved) versus "logged, not blocking, the forward test itself will reveal what matters more honestly than another audit pass." Started the day's actual CORE Scanner cycle (VIX 16.73, STANDARD regime, all 20 CORE contract IDs resolved) but paused before the IVR-screening step and closed the session instead, given how much the audit work had already consumed — the scan itself is queued fresh for next session, not abandoned.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| MEDIUM (finding, cross-repo, unblocked not fixed) | `resolve_trade` / `PATCH /journal/resolve/<id>` silently no-ops on a nonexistent `trade_id` | Session 27: found while stress-testing the new resolve endpoint — calling it with a bad ID returns `200 {"status":"success"}` with nothing actually changed, since `resolve_trade` only runs its `UPDATE` inside `if row:` and never checks `cursor.rowcount`. Low urgency (not wired into the daily workflow yet) but worth a one-line fix (check `rowcount`, 404 if nothing matched) before this runs unattended for months. Not yet relayed as its own HANDOFF — noted here and in `HANDOFF_gemini_forward_test_resolve_endpoint.md`'s discussion. |
| HIGH (finding, cross-repo, unblocked not fixed) | `FWD_TEST:` positions can be closed via the Gemini dashboard's own Close button, bypassing the forward test's mid/close-of-day resolution methodology | Session 27, Jul 16, 2026, real incident, not hypothetical: `backend.log` shows `PUT /journal/close/8` and `PUT /journal/close/15` at 15:16 ET — AFRM and AVAV closed via the dashboard's native "STOP HIT" button, at intraday `last` price (not bid/ask mid) and not at a close-of-day checkpoint. Confirmed `/journal/close` and `/journal/update` are both narrow-update (no null-overwrite risk, same shape as `resolve_trade`) — this isn't a data-safety bug, it's a methodology bypass. Accepted the two actual exits as real per Bala's explicit call (a human took a real action) rather than reversed; `forward_test_log.csv` updated with both, flagged with a `RESOLUTION METHODOLOGY DEVIATION` note so it's documented, not hidden. **Fix requested, not yet verified:** `HANDOFF_gemini_fwd_test_close_lockdown.md` asks Gemini to guard both endpoints against `FWD_TEST:` rows, forcing all closes through `/journal/resolve`. Until confirmed live, treat the dashboard's Close button as still able to bypass this on Gemini's side. **Re-verified Session 29 (Jul 20, 2026):** still not fixed — read `options_iq_gemini/app.py:1080-1125` live again, `journal_update` and `journal_close` are unchanged from Session 27, no `FWD_TEST:` check on either. Two real open positions (INFQ, NIO) currently carry sizable unrealized gains (+13.45%/+43.48%) and sit exposed to this exact bypass — this is the actual priority item to relay to Gemini's dev session, not a theoretical risk. |
