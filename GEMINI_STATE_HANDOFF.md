# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-16 16:11:15
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `skill-options-scanner.md`: Options IQ — Autonomous Scanner (v2.3 — Curated Edge Monitor)
- `skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 16, 2026 — Session 27
**The forward test got its first real resolution cycle, a real off-protocol close incident, and its first two pieces of actual automation — plus a full three-round cross-repo verification cycle with Gemini that caught two overstated claims and two real hardcoded-fact bugs.**
- **Session-start cross-repo check found a real, live discrepancy:** Gemini's dev session had submitted `HANDOFF_hub_iv_hv_units_resolved.md` claiming the Session 26 `iv_hv_ratio` units bug was fixed. Verified directly rather than accepted — read `app.py:743` (`>= 100.0`, confirmed), ran `test_centaur_contract.py` (3/3) and `test_session21_relay.py` myself. Genuinely fixed. But cross-checked against Gemini's own `STATE_HANDOFF.md` and found it still showed the old `>= 1.0` threshold — the regen script hadn't picked up the fix. Known Issues row flipped to RESOLVED with the real evidence.
- **Gemini then reported a Phase 16 session close ("rock solid and mathematically verified")** — ran it through `skill-cross-repo-fix-verification.md` rather than relaying it. The real work (`math.sqrt(252)` annualization fix, Gamma Velocity dynamic trailing stop, `last_gamma` DB migration, `test_quant_math.py` 11/11) checked out genuinely. Three things didn't: an `AUDIT.md` claim about a "simulated-data 503 abort" that didn't exist anywhere in the code (grepped and confirmed), a `history.md` phase-ordering bug that made `generate_handoff.py` grab stale Phase 15 content as "latest" even after Phase 16 was added, and nothing was actually committed despite "locked down." **Three round-trips of drafting a finding → Gemini fixing it → me re-verifying live**, not just reading the second summary: false claim removed (verified via `git show`), phases reordered (verified via `grep`), real commit landed (`d649ad5`). A fourth, smaller round caught a `scripts/generate_handoff.py:91` hardcoded gate-threshold literal — fixed and verified (commit `e475bd5`).
- **Explained "daily marks" to Bala, then had to correct myself:** first pass assumed the hub needed to independently pull Tradier quotes for the close-of-day check. Bala pointed at Gemini's own live dashboard, which was already showing this. Re-read `/journal/monitor` directly and found it genuinely is live (fresh Tradier pull every poll, not cached) — but confirmed it's advisory-only by design, never writes `status` back to the DB. Corrected the framing: the hub's job isn't re-fetching quotes, it's applying the close-of-day resolution rule and actually executing the close, which nothing did automatically yet.
- **Ran a full live PATH B Scanner cycle** (VIX 16.64, STANDARD; CORE 0/20 again, all failed Sieve 1; EXTENDED 64/65 resolved, ABB unresolvable again) — 85 individual MCP calls, by hand. Two Sieve-1 survivors that mattered: **XLF**, already an OPEN REJECT from Session 26 (IV/HV was 101.7% then), re-screened today at 98.7% — a genuine cross-group migration, logged as a **new SURVIVOR** (id 21) per the protocol's explicit migration rule while the original REJECT (id 14) stayed open untouched. **JD**, a new REJECT (id 22, failed Sieve 2b at 102.1%). **HIVE** passed Sieve 1 but failed Gate C on liquidity ($83.2M/day); Directional Builder then came back genuinely MIXED (3 bullish/2 bearish/1 unscored of 6) — recorded as `BUILDER_MIXED` in the CSV, never logged to Gemini's journal, per the protocol's anti-survivorship rule. AFRM and NIO were repeat finalists/rejects, correctly not re-entered (idempotency).
- **Discovered a real off-protocol close, live, mid-session:** checking AFRM/AVAV near close found both already `CLOSED` in Gemini's journal — `backend.log` showed `PUT /journal/close/8` and `/15` at 15:16 ET, the Gemini dashboard's own "STOP HIT" button, using intraday `last` price (not the protocol's mandated bid/ask mid at close-of-day). Not a bug on Gemini's side (`/journal/close` is narrow-update, same safety shape as the resolve endpoint built earlier) — a real methodology bypass. **Bala's call: accept both exits as the real recorded outcome** (a human took a real action) rather than reverse them — `forward_test_log.csv` updated with a `RESOLUTION METHODOLOGY DEVIATION` note on both rows so the deviation is documented, not hidden.
- **Built the first two pieces of real forward-test automation, both tested live before being trusted:**
  1. `resolve_positions.py` — pulls live Tradier mids for every open `FWD_TEST:` position, compares against each position's own stored stop/target, resolves STOP/TARGET/TIME, calls Gemini's journal, updates the CSV. Caught and fixed two real bugs in my own code during testing (a skip-condition that read the wrong CSV column, silently ignoring every row; a half-dollar-strike truncation bug that would've broken XLF's 56.5 strike specifically) before running it live. **First real live run: MP, URA, OKLO all resolved TARGET** (+70.14%, +71.05%, +79.61%), verified against Gemini's journal directly, not the script's own printout.
  2. `build_and_log.py` — takes a Scan-output CSV and automates everything downstream: dedupe/migration detection, direction inference (Tradier-only OHLCV — SMA200/EMA-stack/YTD/52w-range/today's-P/C, a reduced signal set vs the full MCP-based skill since avg-90d P/C has no Tradier equivalent, documented not hidden), contract construction (21–35 DTE window with the established overshoot-exception rule, nearest ATM strike, quote-or-skip), journal logging, and the CSV row. Tested end-to-end against real data (dedupe on GDX, migration detection on NIO — correctly traced to its existing REJECT — a real build+log+verify+cleanup round-trip on TSM) before being called done.
- **Locked resolution down to hub-only, and set a real profit-take rule:** per Bala's explicit call, `FWD_TEST:` positions may only close via the hub's own scripts going forward (`HANDOFF_gemini_fwd_test_close_lockdown.md` requests the Gemini-side guard, not yet verified live). Target changed from entry mid × 1.60 (+60%) to **× 1.80 (+80%)** — confirmed via three scoped questions rather than guessed, and explicitly **non-retroactive**: the 12 positions logged before today keep their original × 1.60, everything `build_and_log.py` logs from today on uses × 1.80. No DB rows were edited to enforce this — each trade's own stored `target_price` already encodes the correct era.
- **Running tally after today's resolution + build work: 14 total positions, 5 resolved (2 STOP, both SURVIVOR — AFRM −52.99%, AVAV −38.42%; 3 TARGET, all REJECT — MP +70.14%, URA +71.05%, OKLO +79.61%), 9 open.** Flagged explicitly as too small a sample to mean anything about the actual hypothesis (does gate-passing predict outcome) — but three REJECT wins and two SURVIVOR losses in a row, opposite the hypothesis direction, is the kind of pattern worth watching rather than dismissing once n grows.
- Five HANDOFF docs drafted this session for Gemini's dev session: `HANDOFF_gemini_phase16_close_findings.md`, `HANDOFF_gemini_state_handoff_hardcoded_gate.md` (both already actioned and verified), `HANDOFF_gemini_forward_test_resolve_endpoint.md` (actioned, verified), `HANDOFF_gemini_fwd_test_close_lockdown.md` (sent, not yet verified — Bala to paste into Gemini's next session).

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| MEDIUM (finding, cross-repo, unblocked not fixed) | `resolve_trade` / `PATCH /journal/resolve/<id>` silently no-ops on a nonexistent `trade_id` | Session 27: found while stress-testing the new resolve endpoint — calling it with a bad ID returns `200 {"status":"success"}` with nothing actually changed, since `resolve_trade` only runs its `UPDATE` inside `if row:` and never checks `cursor.rowcount`. Low urgency (not wired into the daily workflow yet) but worth a one-line fix (check `rowcount`, 404 if nothing matched) before this runs unattended for months. Not yet relayed as its own HANDOFF — noted here and in `HANDOFF_gemini_forward_test_resolve_endpoint.md`'s discussion. |
| HIGH (finding, cross-repo, unblocked not fixed) | `FWD_TEST:` positions can be closed via the Gemini dashboard's own Close button, bypassing the forward test's mid/close-of-day resolution methodology | Session 27, Jul 16, 2026, real incident, not hypothetical: `backend.log` shows `PUT /journal/close/8` and `PUT /journal/close/15` at 15:16 ET — AFRM and AVAV closed via the dashboard's native "STOP HIT" button, at intraday `last` price (not bid/ask mid) and not at a close-of-day checkpoint. Confirmed `/journal/close` and `/journal/update` are both narrow-update (no null-overwrite risk, same shape as `resolve_trade`) — this isn't a data-safety bug, it's a methodology bypass. Accepted the two actual exits as real per Bala's explicit call (a human took a real action) rather than reversed; `forward_test_log.csv` updated with both, flagged with a `RESOLUTION METHODOLOGY DEVIATION` note so it's documented, not hidden. **Fix requested, not yet verified:** `HANDOFF_gemini_fwd_test_close_lockdown.md` asks Gemini to guard both endpoints against `FWD_TEST:` rows, forcing all closes through `/journal/resolve`. Until confirmed live, treat the dashboard's Close button as still able to bypass this on Gemini's side. |
