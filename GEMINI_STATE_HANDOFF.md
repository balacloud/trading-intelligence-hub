# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-27 14:25:35
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `docs/specs/OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `docs/skills/skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `docs/skills/skill-options-scanner.md`: Options IQ — Autonomous Scanner (v3.1 — Watchlist-Paste Edge Monitor)
- `docs/skills/skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `docs/skills/skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 27, 2026 — Session 36

**The IV Rank question closed for real via IBKR's human agent + independent doc verification; a full IBKR-vs-Tradier field-availability artifact built, then corrected mid-session after overclaiming; two real forward-test runs (first-ever successful PATH B via the deterministic pipeline, surfacing a load-bearing `sieves.py` bug); VIX live-fetch and entry-Greeks logging wired into `build_and_log.py`; Gemini's own forward-test analysis verified and cross-referenced against this hub's existing findings.**

Opened with `skill-session-start.md` (clean orientation, Monday, markets open, VIX STANDARD) and the mandatory cross-repo check (nothing drifted since Session 34's stale-skill-path finding). Bala then pasted IBKR support's human-agent reply (Steve D., confirming the Session 35 ticket-bot's "not in our documentation" answer) — rather than accept it at face value, fetched IBKR's own public field-list page directly, confirming independently that no IV Rank/Percentile field exists anywhere in the canonical documented list. **This closes the IV Rank REST question for good** — three independent sources (live re-probe, official docs, human support) now agree. One small side-thread: the reply also confirmed `7633` (per-strike IV) as real and documented, contradicting Session 35's empty probe result — flagged as likely a wrong-conid issue, resolved later this session (see below).

Bala then pasted IBKR's raw Market Data Fields documentation table and asked for it saved for reference (`IBKR_MARKET_DATA_FIELDS_OFFICIAL.md`, new file) and a "complete data table of what is available and what is not" for visual understanding — built and published a filterable HTML artifact (69 fields, 3-way status: Available/Not Available/Untested). Bala then asked to test the Untested bucket against a real option conid rather than a stock conid, with the IBKR gateway freshly authenticated. **Caught a real bug in the probe code itself before trusting any result:** `/iserver/secdef/info` with a bare month (e.g. "AUG26") returns every expiry in that calendar month, not one contract — the first pass silently resolved to a ~7 DTE weekly instead of the intended ~25 DTE monthly, caught only by cross-checking the resolved premium against Tradier's live quote and finding them far apart. Fixed `client.py`'s `option_conid()` to require an explicit `maturityDate` match. With the correct contract, 7 of 9 untested fields resolved as genuinely available (Delta/Gamma/Theta/Vega/per-strike IV/Break Even/Profit Probability, all cross-checked against Tradier's live Greeks for the identical contract), 1 available-but-mislabeled (`7694` "Probability of Max Return" returns `∞`/huge numbers, not a real probability), 2 confirmed genuinely absent even with a correct conid (`7700`/`7702`).

Bala then asked whether Tradier and IBKR REST could be used interchangeably — walked through the actual operational asymmetry (Tradier is headless/static-token, already unattended in `build_and_log.py`; the direct IBKR gateway needs an interactive browser login every session and times out on ~15-20 min idle) and, on request, sketched a role split field-by-field. Tracing it against real Tradier responses (pulled live, not assumed) showed Tradier alone directly covers or can derive 39 of the 41 fields IBKR provides — the direct REST gateway has no live-pipeline role Tradier + existing IBKR MCP tools don't already cover. Added a Tradier-coverage column to the artifact. **Overclaimed several rows as "already derived" without checking the actual `.py` files first** — caught when Bala pushed back ("do we have formulas for deriving... already there in py scripts"). Re-verified by grepping live: most were false (no historical-vol function, no EMA(200/100/20), no Price/EMA ratios, no Break Even calc exist anywhere in this codebase) — corrected the artifact honestly (Direct / Derived-live / Derivable-not-built / No / N/A), and logged the lesson plainly rather than quietly fixing it: don't write "already implemented" into any doc without grepping the code first, the same standard this hub already holds Gemini's claims to.

**Two real forward-test runs, both through the actual deterministic pipeline, not by hand.** PATH A (50-name `Spec_Compliant_Screener` paste): Gate C eliminated 37/50 on liquidity alone; finalists AVAV/MXL/CAG, but AVAV and CAG dedupe-skipped (already OPEN since Sessions 23-24) — only MXL was a fresh log. Near-miss REJECT candidates hit an unusually high earnings-hard-skip rate (5 straight exclusions — CACC/MGY/KYMR/SSRM/GIL — before MSM/ALV/LW cleared). **PATH B (64-name HUB_EXTENDED paste) — the first time `sieves.py` had ever actually been run against a real PATH B paste.** It failed completely: every row came back `UNSCREENABLE`, because `paste_parser.py` always sets `dollar_vol_usd=None` for PATH B (no dollar-volume column exists in that format) but `sieves.py`'s Gate C treated a missing value as unscreenable rather than curation-satisfied like Gate A — a real divergence between `sieves.py` (built Session 30) and `skill-options-scanner.md`'s own v3.0 conversion (built the same session), never reconciled until now. **Fixed:** Gate C now mirrors Gate A's treatment of a missing value; `OPTIONS_SIEVE_SPEC.md` corrected to match; 2 tests updated. Rerun cleanly: finalists NFLX/GDX/DRAM, with NFLX/GDX dedupe-skipped (OPEN since Session 31) and DRAM logged fresh, plus 3 REJECT-side names (XPEV/HPE/LI, CEG excluded on earnings). **Per Bala's real-time push ("you need to fetch VIX irrespective of where I put it, find a way"):** added `fetch_vix_regime()` to `build_and_log.py` (live Tradier `/markets/quotes?symbols=VIX`), overriding whatever the paste does or doesn't carry — makes the open "does PATH A support a VIX row" question moot rather than answering it. 6 new tests. Manually corrected the day's first 7 CSV rows (logged before the fix existed) from `UNKNOWN` to the real `STANDARD` regime.

**Per Bala's explicit request ("do both, fix artifact and wire up greeks logging"):** `build_and_log.py`'s entry-contract chain call had hardcoded `greeks: "false"`, discarding real Delta/Gamma/Theta/Vega/IV Tradier already returns at zero extra API cost — flipped to `"true"`, captured into a new `entry_greeks` field, logged into CSV notes via a null-safe `format_entry_greeks()` (never fabricates a missing Greek as `0.0`). 5 new tests; verified live against a real XOM contract before calling it done. **133/133 tests passing, up from 122.**

Gemini's own dev session sent an unprompted forward-test analysis (four named trades, proposing "respect the SMA200 primary trend, scrutinize narrow contradicting votes"). Verified every cited number directly against `forward_test_log.csv` before crediting any of it — all accurate. Found this converges with `FORKING_PATHS_LOG.md` Entry 4 (Jul 24's own denominator-complete split: 0/5 UPTREND-call wins vs. 7/11 DOWNTREND-put wins) via an independent method, three days apart, without Gemini having seen it — but Gemini's clean story doesn't explain CAG (trend-aligned, still stopped out -37.5%). Logged as Entry 5, crediting the convergence and naming the counter-example, still **not locked** at n=18 resolved (unchanged from Entry 4 — nothing new resolved this session, only new OPEN positions added). Updated `SKILL_CONVERSION_SCOREBOARD.md`'s Track 2 with both real fixes (`sieves.py` Gate C, `build_and_log.py`'s VIX/Greeks additions) and the current 133/133 count.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| LOW (finding, cross-repo, drafted not sent) | `options_iq_gemini`'s `PROTOCOL.md`, `.agents/AGENTS.md`, and `Docs/CLAUDE_MCP_SKILL_HANDOFF.md` all point at this hub's skill files via stale absolute paths | Session 34 (Jul 25, 2026): found while sweeping for stale references during the docs reorg (root-level files moved to `docs/{handoffs,skills,specs,planning}/`). Two separate problems: the path moved (needs a `docs/skills/` segment), and the filenames were already wrong independent of the move (`skill-ibkr-radar.md`/`skill-directional-builder.md` — missing "options-", predates the June 30, 2026 naming standardization). Purely cosmetic/navigational, nothing runtime-affecting. Drafted `docs/handoffs/HANDOFF_gemini_stale_skill_file_references.md` — not fixed here since `PROTOCOL.md` is read-only for Claude and the others belong to a separate repo. Not yet sent/pasted into Gemini's dev session. |
