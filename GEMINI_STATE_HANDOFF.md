# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-29 13:22:49
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `docs/specs/OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `docs/skills/skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `docs/skills/skill-options-scanner.md`: Options IQ — Autonomous Scanner (v3.1 — Watchlist-Paste Edge Monitor)
- `docs/skills/skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `docs/skills/skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 29, 2026 — Session 38

**A real dual-class-ticker parser bug found by running fresh live data, two full forward-test scan cycles, and the hub's first standalone advisory tool — built, pushed back on, and permanently scoped by Bala mid-session.**

Opened by running a real 50-row PATH A `Spec_Compliant_Screener` paste through `run_scan.py` — 48 screened, not 50, no error. Root cause: `paste_parser.py`'s `TICKER_LINE_RE` never matched dual-class share tickers with a space ("BF A"/"BF B"), and the fallback loop discarded all three lines of each row as unrecognized chrome rather than raising — a real, previously undiscovered contradiction of the module's own "never drops a row silently" docstring claim. Fixed with a second regex plus space-to-slash normalization, confirmed live against Tradier that `BF/B` (not `BF.B`/`BF-B`/`BF B`/`BFB`) is the only form that actually resolves a quote. Same session, found and committed a pre-existing uncommitted `resolve_positions.py` batch from Jul 28 (10 resolutions) that had been sitting in the working tree since before the session started — same "uncommitted resolution batch" pattern Session 37 caught once already. Ran PATH A and PATH B live end-to-end, logging UHS (SURVIVOR), LI (REJECT), NFLX (SURVIVOR), POET (REJECT) — forward test now at **17 SURVIVOR resolved (4 TARGET/13 STOP) / 17 open, 18 REJECT resolved (9/9) / 26 open**, 159→159 tests passing (parser fix, no regression).

**Built the Sector/Theme Advisory Panel** (`research/sector_advisory/`) after Bala reviewed the open DRAM position (id 66, BULLISH on a thin 2/2-signal vote) against his own live IBKR screenshot showing the whole semis/materials complex red that day — a real "as a human I'd have taken the bearish side" moment. Researched rather than assumed: `options-iq`'s `PlaybookTab.jsx` turned out to be general macro patterns, not sector-relative-strength; `options-iq`'s own `sector_scan_service.py`/`/api/sectors/scan` is **deprecated (Day 57)**; the actual live source is `swing-trade-analyzer`'s `/api/sectors/rotation` — confirmed reachable, read its code directly rather than trusting a prior audit memory. Used `EnterPlanMode` for the design (Bala's explicit ask), landed on: extract the skill file's already-hand-curated Ticker→theme labels (`docs/skills/skill-options-scanner.md`), compute a new sub-industry RS-ratio layer via Tradier for clusters STA's 11-broad-sector view can't reach, cross-reference against `forward_test_log.csv`'s open positions — fully standalone, zero coupling to `run_scan.py`/`build_and_log.py`. First real run found two bugs by actually running it: a calendar-day/trading-day conflation that falsely flagged every mature proxy as short-history, and a sync-guard test (built proactively, not after the fact) that caught `ABB`/`SOXX` missing from the cluster mapping before the first live run.

**Bala's direct pushback — "did you even do your due diligence" — led to a real second pass, not a defensive one.** The first cut left 12 of 20 clusters at "no proxy" just because nothing happened to already be on the watchlist; Energy (no XLE) and Defense (no ITA) were the clearest, most indefensible misses. Verified real candidate ETFs live against Tradier (existence + volume, not memory) for every cluster — 19 of 20 now have a defensibly-researched proxy, only POET (photonics, genuinely no fitting ETF) stays proxy-less. Also split a muddled "China ADR/EV" cluster into two honest ones and fixed a mislabeled RIVN (an EV maker, not a space name). Re-run surfaced 2 more real flags beyond DRAM: HPE and PATH.

**Reviewed STA's own sector-rotation code directly** (not from the Session 28 audit memory) when Bala asked how well-built it was: real, resilient, self-honest about its own "not standard RRG" normalization choice (already self-audited and deliberately left as-is on their own Day 69 review) — but genuinely untested (grepped every `test_*.py` in that repo, found none for this endpoint) and yfinance-sourced where this panel is Tradier-sourced. Fixed via the hub's established cross-repo pattern: drafted `docs/handoffs/HANDOFF_sta_sector_rotation_test_coverage.md` with a complete test script matching STA's own convention, **verified live against their actual running server before handing it off** (9/9 checks passed) rather than just proposed — and built `provider_divergence.py` locally (no STA change) comparing the 5 tickers that are simultaneously a hub proxy and an STA broad sector on momentum *sign only*, verified algebraically to be invariant to the two sides' different normalization conventions. Asked next whether that normalization difference actually mattered for this use case — checked live rather than reasoned abstractly: XLF read "Leading" (STA) vs "Improving" (hub) simultaneously, same ticker, same day, momentum agreeing in sign both times — confirmed real, then surfaced it on the page itself (an inline "cf. STA" note plus a footer explanation), not left in a code comment only a future session might find.

**A near-drift caught in real time, not after.** Once the panel existed and worked, Bala reasoned aloud from it toward treating the sector read as an actual selection signal ("dont trade against the market... calls work, puts work because this is bullish or bearish") — recognized as the same open question `FORKING_PATHS_LOG.md` has tracked since Session 30, already showing a compelling-looking split narrow hard as sample size grew (0%/64% → 22%/54%). Surfaced that connection before it became an implicit rule change. **Bala's explicit, unprompted close: "keep it advisory only for now, its purely advisory only for now and for ever aswell"** — recorded three ways (TRADER_LENS Feedback Log, a standing feedback memory, and a permanent statement in `generate_sector_advisory.py`'s own docstring) specifically so a future session with a fuller forward-test dataset doesn't treat "more data now" as authorization this decision already rejected. Closed the loop with a practical workflow fix (`refresh_and_open.command`, a double-clickable regenerate-and-open script) after explaining plainly why a button embedded in the static HTML itself can't do this (browser sandbox blocks a local page from running Python or reading the Tradier token) and why publishing it as a live Claude Artifact wouldn't work either (can't reach `localhost:5001`/local secrets from the cloud).

Closed by running `resolve_positions.py --dry-run` (informational only, matching the established once-daily close-of-day methodology — market was mid-day, not run for real) to answer "what's likely to resolve today": 7 positions already past their line (PATH/DRAM/MSM/XBI/DNTH stops, WEX/CCJ targets), 4 close near-misses flagged (AVAV/HPE/UUUU/GDX). One untracked artifact left alone at close, not committed: a macOS Finder alias file (`refresh_and_open.command alias`) that appeared in the working tree, evidently created by Bala's own Finder action, not this session's — per this hub's own "don't commit files you don't recognize" rule.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| PENDING (cross-repo, drafted not yet relayed) | STA's `/api/sectors/rotation` has real RS-ratio/momentum/quadrant computation but zero automated test coverage | Session 38 (Jul 29, 2026): found while building the hub's new Sector/Theme Advisory Panel, which pulls this endpoint live for broad-sector context. Grepped `swing-trade-analyzer/backend/tests/` and every `test_*.py` in that repo — no coverage of this endpoint anywhere, unlike the rest of the repo's own test files (`test_categorical_comprehensive.py`, `test_verdict_parity.py`). Not a bug in the endpoint itself — its "swing-trading variant of RRG, not standard de Kempenaer RRG" self-documentation checked out fine, and matches STA's own Day 69 self-review ("Bug 0E-F," deliberately left as-is). Drafted `docs/handoffs/HANDOFF_sta_sector_rotation_test_coverage.md` — a complete, ready-to-drop-in test script matching STA's own established convention (standalone script against the live server, no pytest/mock dependency STA doesn't otherwise use), **verified live against the real running `localhost:5001` server before handoff** (9/9 checks passed), not just proposed. Not yet relayed into STA's own dev session. |
