# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-22 15:44:52
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `skill-options-scanner.md`: Options IQ — Autonomous Scanner (v3.1 — Watchlist-Paste Edge Monitor)
- `skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 22, 2026 — Session 31

**Finished what Session 30 paused (`HUB_EXTENDED`, VIX rows), expanded the watchlist column set, ran a full live forward-test cycle end-to-end on real EXTENDED-scan output, and caught a genuine one-row data-integrity bug by actually checking a user's "this looks empty" observation instead of assuming it was nothing.**

Session-start's mandatory cross-repo check (the open Universal-Scanner-never-checks-IVR row) verified against Gemini's live `STATE_HANDOFF.md` (Phase 18, Jul 21) — still open, not stale. Gemini's own Phase 18 was documentation-only, re-confirming the same gap and aligning on the Centaur Hub as the manual-verification layer, but no code fix landed either side.

**Discussed scanning/discovery capability broadly** (IBKR MCP, Tradier, screenshot input, the Chrome extension, HUB_CORE/EXTENDED) — clarified PATH A (dynamic TWS/Client-Portal scanner) is the only real *discovery* engine; the curated watchlists are *monitoring*, not discovery. Explored automating PATH A via the Chrome extension; confirmed Bala runs the scanner through Client Portal (not desktop TWS, which would've made this infeasible) but parked the idea by his own call to focus on watchlist parameter expansion instead.

**Added 4 new IBKR watchlist columns** (Put/Call Volume, Opt Volume Change %, Price/EMA(50), Opt. Imp. Vol. Change) — pulled from `options-iq`'s companion doc but explicitly not its thresholds, each run through Alex's one-sentence-edge test individually rather than ported wholesale. 3 wired as flags/context (`skill-options-scanner.md` new Step 2.4); the 4th (IV direction) left deliberately unwired — no validated buying-context read exists yet, display-only until live-observed. `skill-options-scanner.md` v3.0→v3.1, `IBKR_SCANNER_WATCHLIST_SETUP.md` updated to match.

**Built `HUB_EXTENDED` for real** (id 111) — the exact task Session 30 paused on for context-cost reasons. Delegated to a background subagent specifically to keep `search_contracts`'s noisy per-ticker output out of the main session, which is the same problem that caused the pause last time. 64/65 resolved; `ABB` unresolvable a 4th confirmed time (OTC-only since 2018). Verified via a live `get_watchlist` pull rather than trusting the subagent's report alone.

**Verified the new columns against a real live paste, not just a schema check.** Bala pasted the actual `HUB_EXTENDED` watchlist with headers; ran the real Sieve 1/B/OI/2b logic by hand across all 64 names — 16 Sieve-1 survivors, 6 clearing the IV/HV<100% edge, top 3 NFLX/GDX/DRAM. Caught a real gap in the process: the `Underlying Price` column returned `—` (blank) for every single row — a genuine config gap, not cosmetic, since it feeds the RANGE calc. Used `Last` as a documented stand-in rather than silently substituting it.

**Ran the full forward-test daily procedure end-to-end on real output for the first time** — Scanner → dedupe → Directional Builder (full technical stack, not just the scan-time EMA numbers) → contract construction via Tradier → quote-or-skip → Centaur POST for survivors → journal logging → CSV update, on all 6 EXTENDED candidates (survivors NFLX/GDX/DRAM, near-miss rejects POET/PATH/CCJ). Delegated to a background subagent for the same context-cost reason as the watchlist build. Result: 5 logged clean (ids 35-39), PATH correctly stood down as `BUILDER_MIXED` (an exact 3-3 signal tie) rather than forced into a group. CCJ migrated SURVIVOR→REJECT (its earlier position, id 11, already closed) — allowed and logged per protocol. Gemini's actual LLM was online this run (no deterministic-fallback banner); for DRAM specifically it explicitly recommended **NO TRADE** (theta burn, IV>106%, no kinetic confirmation) but the position still got logged mechanically since Centaur's read is metadata, not a veto, per the protocol's own anti-fantasy-fill rule. Forward test now stands at 13 SURVIVOR (6 resolved: 3 STOP/3 TARGET, 7 open) / 11 REJECT (5 resolved: 2 STOP/3 TARGET, 6 open) — both still well short of the pre-registered n≈30/group; walked Bala through why individual-trade resolution speed (fast, by design — 21-35 DTE with a time-stop at 60%) doesn't shorten the timeline to a real result, since the actual bottleneck is new-candidate logging rate, not exit speed. Protocol's own pacing estimate (Oct-Nov 2026 for Checkpoint A) stands uncorrected.

**Found a real, isolated data-integrity bug by checking Bala's "UUUU doesn't show anything" observation directly instead of assuming it was nothing.** Pulled UUUU's option quote straight from Tradier — genuinely live (bid $1.25/ask $1.45, OI 233), ruling out a market-data gap. Compared `occ_symbol` format across 5 other journal rows: every other position stores Tradier's compact format (`NFLX260821P00070000`); id 33 alone stores the OCC-padded form with two literal internal spaces (`'UUUU  260814P00013000'`) — from Session 30's original logging, predating the deterministic pipeline. `/journal/monitor`'s skip-on-missing-quote logic (Session 21 hardening) is working exactly as designed; the input feeding it is wrong. Confirmed the same bug will hit the hub's own `resolve_positions.py` too (false `NO_QUOTE`, not real illiquidity). Per Bala's explicit call, drafted `HANDOFF_gemini_uuuu_occ_symbol_fix.md` (a one-row SQL fix, plus a suggestion to check `trades.db` for other rows with the same malformation) rather than track-as-exception or attempt a same-repo write — `/journal/update`'s field-nulling risk (G4) rules out an API-side fix, and cross-repo boundary rules out a direct DB write from here.

**Real near-miss on scheduling infrastructure.** Attempted to use `/schedule`'s cloud-routine mechanism to auto-run `resolve_positions.py` near close; caught mid-setup (before creating anything) that cloud routines run in an isolated sandbox with no access to `localhost:5002` (Gemini's local Flask server), the local Tradier token, or the local CSV — none of which resolve_positions.py can function without. Backed out cleanly, corrected to a manual-ping approach. `resolve_positions.py`'s actual near-close run is still pending as of this close — queued, not done, don't let this session's other work imply otherwise.

**Closed a real gap in both live watchlists:** neither `HUB_CORE` nor `HUB_EXTENDED` had a VIX row, so today's `HUB_EXTENDED` paste came back "regime UNKNOWN." Resolved directly via MCP (`search_contracts` → the correct CBOE Volatility Index, `IND` type, contract_id 13455763 — not one of the ~15 VIX-named ETF/ETN look-alikes in the search results) and added it to both live watchlists via `edit_watchlist`, verified via a fresh `get_watchlist` pull on both. Column-level display (LAST) needs no separate Client Portal step since it's a default column, not one of the custom ones being added.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| LOW-MEDIUM (finding, cross-repo, unblocked not fixed) | UUUU (journal id 33) has a malformed `occ_symbol` (`'UUUU  260814P00013000'` — OCC-padded, two spaces — vs every other row's compact Tradier format like `NFLX260821P00070000`) | Session 31 (Jul 22, 2026): Bala noticed UUUU showing nothing in the dashboard monitor. Verified live, not assumed: pulled UUUU's option quote directly from Tradier (genuinely live — bid $1.25/ask $1.45, OI 233), confirming this is a stored-symbol-format bug, not a market-data gap. `/journal/monitor`'s skip-on-missing-quote logic (Session 21 hardening) is working correctly — it's the input that's wrong. **Operationally relevant now:** the hub's own `resolve_positions.py` will report `NO_QUOTE` for UUUU (false negative) until fixed — it keys quotes by the same malformed `occ_symbol`. Drafted `HANDOFF_gemini_uuuu_occ_symbol_fix.md` (one-row `UPDATE trades SET occ_symbol = 'UUUU260814P00013000' WHERE id = 33`, plus a suggestion to grep `trades.db` for any other rows with the same padding pattern). Not fixed yet — needs Gemini's dev session. |
| HIGH (finding, cross-repo, unblocked not fixed) | `GET /scan/universal` (the TradingView-based "Universal Quant Sieve") never checks IVR at all — its own code admits it | Session 30 (Jul 21, 2026): read `options_iq_gemini/app.py:543-678` live after Bala flagged a screenshot showing F/GM/AMD as "3 PASSED / high-conviction leads." Confirmed: the endpoint returns `"iv_rank": 0, # Sentinel: Manual verification needed via Hub` (line 651) — Sieve 1 (IVR ≤ 45), the gate this hub's entire buying-edge thesis depends on, is never evaluated by this scanner. **Proven to matter, not theoretical:** AMD, shown as a bullish lead on the screenshot, has a real IVR of 98.80% per this same session's own live CORE scan (`sieves.py` fixture data, IBKR MCP) — more than double the ceiling, second-worst of all 20 CORE names. Separately, the HV side of its own IV/HV computation (`scan_queries.py`'s `Volatility.D` field × `√252`) is unofficial-API-sourced and never independently cross-checked the way this hub's IBKR-based HV was — GM's displayed HV (110.99%) is implausibly high for a mega-cap automaker, and only 3 of 50 candidates failed the HV>IV mismatch gate telemetry-wide, inconsistent with how rarely a real buyer's edge appears in this hub's own vetted data (CORE typically 0-1/20 survivors). **Not fixed:** would need either (a) Gemini wiring a real IVR check into this endpoint, or (b) treating its output as raw candidate tickers only, always re-screened through the hub's own `sieves.py` before anything gets called "passed" — same pattern already used for Radar-discovered names feeding the CORE/EXTENDED watchlist. **Do not log any Universal Scanner output to the forward test without an independent IVR check first.** **Re-verified Session 31 (Jul 22, 2026) session-start mandatory cross-repo check:** read Gemini's live `STATE_HANDOFF.md` (Phase 18, Jul 21) directly — confirms this is still open, not stale. Gemini's own Phase 18 was documentation-only ("no new code was written"), re-confirming the same IV Rank gap and clarifying the Centaur Hub bridge is the intended manual-verification layer — consistent with this row's own option (b), but no code fix has landed on either side. |
