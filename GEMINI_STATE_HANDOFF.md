# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-21 13:38:51
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `skill-options-scanner.md`: Options IQ — Autonomous Scanner (v3.0 — Watchlist-Paste Edge Monitor)
- `skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 21, 2026 — Session 30

**A long, dense session: corrected a real skill-discovery gap, closed out the last two cross-repo Known Issues (with a genuine deployment-lag discovery in between), rebuilt Scanner to paste-mode per the Session 13 Task E spec, ran the forward test all the way into Gemini's live endpoint for the first time, then — after the user pushed on how much of this was still being done by eye — planned and built the hub's first real deterministic-Python conversion of the sieve/technicals/payload logic, put it through four progressively more stringent self-re-verification passes (each one finding something real the last one missed), and caught a genuine, proven data-integrity gap in Gemini's own TradingView-based Universal Scanner along the way.**

Opened by correcting my own session-start: greeted generically instead of running `skill-session-start.md`, because it and `skill-session-close.md` were loose root-level files, not registered under `.claude/skills/` where Claude Code's discovery actually looks. Fixed by symlinking both into `.claude/skills/session-start/SKILL.md` and `.claude/skills/session-close/SKILL.md` — single source of truth stays the root file, no duplicated content. Confirmed no git remote on this repo (local-only); `options_iq_gemini` does have one (`balacloud/options_iq_gemini`), 7 commits ahead at session start.

**Closed the two remaining cross-repo Known Issues, with a real finding in the middle of verifying the fix.** Gemini had already written (uncommitted) fixes for both the `resolve_trade` silent no-op and the `FWD_TEST:` close-bypass. Verified both properly rather than trusting the summary: read the diffs, re-ran `test_quant_math.py` + `test_centaur_contract.py` (14/14), ran a live functional check against a scratch `FWD_TEST:` row confirming the guard actually 403s. Committed `options_iq_gemini@6f2a20f`. Then, asked to verify the fix was actually *live*, found it wasn't — the running Flask process had started hours before the fix was written, and `backend.pid` held a stale PID, so `./stop.sh` silently missed the real process. Root-caused via `lsof`/`ps`, fixed by manually killing PID 32757 and restarting (new PID 51652), then re-verified against the real running HTTP server this time, not the file. **The actual lesson, worth carrying forward: "committed" and "pushed" are not "live" for a long-running process** — always confirm the running server's start time postdates the fix.

**Converted `skill-options-scanner.md` to watchlist-paste mode (Task E, spec'd Session 13, never built until now) — v2.3 → v3.0.** Input changes from ~22–41 MCP calls/run to a pasted IBKR watchlist table, same pattern Radar already uses. Built `HUB_CORE` as a real, synced IBKR watchlist (id 110, all 20 CORE tickers, via the IBKR MCP `create_watchlist` tool) — `HUB_EXTENDED` (~65 more names) deliberately paused mid-build: each `search_contracts` call returns a large noisy result set, and burning through 65 of them wasn't worth the context cost mid-session. New `IBKR_SCANNER_WATCHLIST_SETUP.md` documents the buying-tuned column setup (mirrors `options-iq`'s doc structure without importing its selling thresholds — the inversion trap flagged explicitly).

**Ran the forward test live, twice, including the first PATH B CORE run driven directly by MCP (paste-mode needs a real IBKR paste, which wasn't supplied).** A user-pasted PATH A scan (PURR/PL/UUUU/ECHO/AFRM) deduped correctly against existing open positions — PL/ECHO/AFRM all already had OPEN positions, consistent with the prior session's identical dedupe on an overlapping paste — leaving only **UUUU** as genuinely new, and it tripped the earnings TBLA gate (Jul 31, 10 days out) after clearing every other gate cleanly. Logged as a REJECT per protocol (rejects still get a full Directional Builder read) — id 33. Separately ran a direct-MCP CORE scan (20 tickers): **NVDA was the sole Sieve-1 survivor** (IVR 40.6%, next-closest ALB at 52.6%), AVGO skipped as genuinely `MCP_DATA_UNAVAILABLE` (missing `implied_vol_underlying`, not fabricated). Built the full CENTAUR payload and **POSTed it live to Gemini's `/analyze/centaur` endpoint for the first time this session** — the actual Gemini LLM was offline, the response headed "DETERMINISTIC QUANT PARTNER FALLBACK ACTIVE," and Centaur's own live Tradier re-pull independently selected a *better* contract (Aug 21 205 Call, OI 20,080) than the one submitted (Aug 14 205 Call, OI 2,189) — logged the system's actual pick, id 34. First CORE (not EXTENDED) survivor of the whole test, and the first entry with genuinely clean liquidity.

**Built real interpretation guardrails after catching myself narrating an 11-trade sample causally.** Added a full Interpretation Guardrails section to `FORWARD_TEST_PROTOCOL.md` — three explicit states (MONITORING/EXPLORATORY/CONFIRMATORY), a forking-paths log requirement, confound tracking (new `vix_regime` CSV column), and a fixed interim-report template with a standing "not yet interpretable" banner. New `FORKING_PATHS_LOG.md` seeded honestly, including the exploratory analysis that prompted the section in the first place. Also wrote a **Path to Live** section — two checkpoints (n≈30/group primary, n≈100/group only if greenlit after seeing the first result), a real decision tree per outcome, and an explicit compound "go live" checklist that treats a statistics pass as one of five conditions, not the whole thing (spread-conditioned fill-quality analysis, real stop mechanism, sizing rules, pilot tranche, and Bala's own sign-off as a required, non-automatic step).

**Planned and built the hub's first real deterministic-Python conversion.** Prompted by the user noticing how much of the Directional Builder/sieve logic was being computed ad hoc in disposable `/tmp` scripts each time. An Opus planning pass, explicitly grounded in `GOLDEN_RULES.md` and run through `PERSONA.md`'s two lenses *before* design (per the project's own standing rule), produced `PLAN_deterministic_pipeline_formalization.md` — Alex's Systems Architect lens vetoed a fetcher-layer design (MCP only exists in-conversation, never in a subprocess) and folding VIX classification into the sieve module; the Quant Trader lens flagged contract selection as the single highest-value item, since `build_and_log.py`'s existing `pick_atm_contract` turned out to be cruder than the manual process it was meant to automate. Phase 5 (wiring into `build_and_log.py`'s live path) deliberately held until the forward test's Checkpoint A, so the primary statistical test runs on one consistent methodology throughout — not a stall, the same discipline already applied to gate thresholds, now applied to the code that builds the trades.

Built Phases 1–4 (`technicals.py`, `sieves.py`, `contracts.py`, `centaur_payload.py`), then ran **four progressively more stringent self-re-verification passes**, each using a genuinely different hunting strategy, each finding something real:
- **Pass 1** (general re-read): caught a stale RSI value in the day's own forward-test log entry (56.0 flat-average vs. 51.7 true Wilder) before it was committed.
- **Pass 2** (adversarial — "what would a careless caller do"): found `build_payload` would crash with a bare `TypeError` on `None < 25`, found `select_contract` silently treated a `MIXED` direction as `BEARISH` and returned a put with no warning, found the delta-band tie-break logic had zero test coverage because today's real chains only ever had 0–1 in-band candidates.
- **Pass 3** (hunt for silent-None-skips specifically, per GOLDEN_RULES' "return null, not a plausible fake"): found `dollar_vol_usd=None` was silently skipping Gate C entirely and letting a name through as a `SURVIVOR` — a real gap distinct from `market_cap_usd=None`, which the spec explicitly documents as an intentional PATH B shortcut.
- **Pass 4** (exact-boundary sweep + a real integration test chaining all three modules with unmodified real data): found **zero new bugs** — a genuinely different result from the first three, meaning the two highest-suspicion remaining areas were already correct, just not yet *proven* correct by a test.

79 tests passing by the end (up from 0). Confirmed `technicals.py`'s Wilder RSI fix isn't cosmetic: UUUU's RSI moves from 28.6 ("oversold," what's in today's committed forward-test note) to 35.4 (neutral) under correct smoothing.

**Closed by catching a real, proven data-integrity gap in Gemini's own Universal Scanner.** Bala showed a screenshot of `/scan/universal` (Gemini's separate, TradingView-unofficial-API-based S&P 500 screener) flagging F/GM/AMD as "3 PASSED / high-conviction leads." Read the live code rather than trusting the screen: `app.py:651` returns `"iv_rank": 0, # Sentinel: Manual verification needed via Hub` — this scanner never checks IVR at all. Proved it mattered with data already in hand from this same session: AMD's real IVR (from the CORE scan a few hours earlier, live IBKR MCP) is 98.80% — nowhere close to the 45% ceiling this hub's entire buying-edge thesis depends on. Logged as a new HIGH cross-repo Known Issues finding with an explicit "do not log Universal Scanner output to the forward test without an independent IVR check first" flag.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| HIGH (finding, cross-repo, unblocked not fixed) | `GET /scan/universal` (the TradingView-based "Universal Quant Sieve") never checks IVR at all — its own code admits it | Session 30 (Jul 21, 2026): read `options_iq_gemini/app.py:543-678` live after Bala flagged a screenshot showing F/GM/AMD as "3 PASSED / high-conviction leads." Confirmed: the endpoint returns `"iv_rank": 0, # Sentinel: Manual verification needed via Hub` (line 651) — Sieve 1 (IVR ≤ 45), the gate this hub's entire buying-edge thesis depends on, is never evaluated by this scanner. **Proven to matter, not theoretical:** AMD, shown as a bullish lead on the screenshot, has a real IVR of 98.80% per this same session's own live CORE scan (`sieves.py` fixture data, IBKR MCP) — more than double the ceiling, second-worst of all 20 CORE names. Separately, the HV side of its own IV/HV computation (`scan_queries.py`'s `Volatility.D` field × `√252`) is unofficial-API-sourced and never independently cross-checked the way this hub's IBKR-based HV was — GM's displayed HV (110.99%) is implausibly high for a mega-cap automaker, and only 3 of 50 candidates failed the HV>IV mismatch gate telemetry-wide, inconsistent with how rarely a real buyer's edge appears in this hub's own vetted data (CORE typically 0-1/20 survivors). **Not fixed:** would need either (a) Gemini wiring a real IVR check into this endpoint, or (b) treating its output as raw candidate tickers only, always re-screened through the hub's own `sieves.py` before anything gets called "passed" — same pattern already used for Radar-discovered names feeding the CORE/EXTENDED watchlist. **Do not log any Universal Scanner output to the forward test without an independent IVR check first.** |
