# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-26 08:35:27
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `docs/specs/OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `docs/skills/skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `docs/skills/skill-options-scanner.md`: Options IQ — Autonomous Scanner (v3.1 — Watchlist-Paste Edge Monitor)
- `docs/skills/skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `docs/skills/skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 26, 2026 — Session 35

**The IV Rank REST question, reopened and definitively closed via a live probe plus a real IBKR support ticket; VIX regime and earnings-date gates wired into the daily paste-driven pipeline.**

Bala shared his live IBKR Market Data Subscriptions screenshot, prompting a full re-check of the Session 32 "IV Rank unavailable via REST" finding rather than re-asserting it from memory. A third-party reference (`areed1192/interactive-broker-python-api`) surfaced field `7195` plus 23 related IV/HV Rank-family fields, all genuinely queried in the original probe but absent from its saved response data. **Per Bala's explicit push-back — "did you review IBKR documents, community threads... before raising this email" — real external research was done before drafting anything**, which is what actually found field 7195 and reframed the question from closed to open in the first place; that discipline (verify a claim hasn't already been publicly answered before asking an external party) is worth keeping for any future support-ticket-style question. A live re-probe (gateway freshly authenticated, real paid subscriptions confirmed active) against 7 tickers with known real pasted values resolved a 3-way field-ID ambiguity — `7283` confirmed correct for "Opt. Implied Volatility %", matching pasted values within 0.1-2.2pt for all 7 tickers — and reconfirmed all 24 IV/HV Rank-family fields plus `7613`/`7634` return nothing live. Bala then filed a support ticket with IBKR including this exact request/response evidence; **IBKR's ticket-bot response confirmed directly: IV Rank/Percentile are TWS-desktop-only chart studies, computed client-side from a local history buffer, and are not available via the API regardless of subscription tier** — a hard platform limitation, matching this hub's own structural theory (IV Rank needs a 52-week daily-IV series; no REST endpoint exposes one) exactly. Human agent follow-up is still pending (~3 business days per IBKR's own note) — treated as strong, not fully final, in every doc touched (`IBKR_REST_API_REFERENCE.md`, this file's Known Issues, `SKILL_CONVERSION_SCOREBOARD.md`).

Separately, walked through what the daily 5-minute PATH A/B paste routine actually covers: confirmed it already feeds Sieve 1/Gate A/C, and that the full technical-signal set plus option-contract selection already run automatically off Tradier inside `build_and_log.py` — no paste needed for either. But earnings and VIX regime were genuine remaining gaps: `earnings.py` existed (built the prior session) but had never been wired into `build_and_log.py`, and VIX regime had no path into the deterministic Python pipeline at all despite already being a documented convention in `skill-options-scanner.md` (add a VIX row to the same watchlist paste). **Wired both this session.** `paste_parser.py` now pulls any ticker-literally-"VIX" row out of either paste format and reports it separately as `VixInfo(level, regime)` at the documented VIX≤25 STANDARD / >25 HIGH-FEAR line, instead of leaving it to be mis-treated as a tradeable candidate by `sieves.py` (`parse_paste()`'s return signature changed to a 3-tuple `(format, rows, vix)`; every caller/test updated). `build_and_log.py` now calls `earnings.get_earnings_status()` right after direction is resolved and before any option-chain call — `HARD_SKIP` (<14 days) stops the build before wasting a live Tradier call on a name about to be rejected anyway; `WITHIN_HOLD`/`UNKNOWN` both flag and proceed, matching the project's existing "flag, don't silently block on a soft signal" convention already used for VIX. **A second, more critical review pass on this same wiring (per the Session 34 three-pass rule) caught a real bug before commit:** `EarningsResult.note` — the specific near-boundary / Finnhub-cross-listing-mismatch caveat text `earnings.py` was built specifically to carry — was being silently dropped in both places it got threaded through into `build_and_log.py`'s output; fixed and locked in with a regression test asserting the note text survives. 122/122 tests passing (was 116 at Session 33's close). The paste → parse → sieve → CSV hand-off into `build_and_log.py` is still a manual/interactive step each morning, not a saved orchestrator script — noted as a known, accepted gap, not treated as fixed.

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| LOW (finding, cross-repo, drafted not sent) | `options_iq_gemini`'s `PROTOCOL.md`, `.agents/AGENTS.md`, and `Docs/CLAUDE_MCP_SKILL_HANDOFF.md` all point at this hub's skill files via stale absolute paths | Session 34 (Jul 25, 2026): found while sweeping for stale references during the docs reorg (root-level files moved to `docs/{handoffs,skills,specs,planning}/`). Two separate problems: the path moved (needs a `docs/skills/` segment), and the filenames were already wrong independent of the move (`skill-ibkr-radar.md`/`skill-directional-builder.md` — missing "options-", predates the June 30, 2026 naming standardization). Purely cosmetic/navigational, nothing runtime-affecting. Drafted `docs/handoffs/HANDOFF_gemini_stale_skill_file_references.md` — not fixed here since `PROTOCOL.md` is read-only for Claude and the others belong to a separate repo. Not yet sent/pasted into Gemini's dev session. |
