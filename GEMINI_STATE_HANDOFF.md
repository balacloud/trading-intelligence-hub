# TRADING-INTELLIGENCE-HUB → OPTIONS IQ GEMINI STATE HANDOFF
> **Auto-generated:** 2026-07-15 11:53:12
> **Purpose:** What changed on the hub side that's relevant to Options IQ Gemini. Read this
> at session start alongside your own STATE_HANDOFF.md -- that file covers your own repo's
> state; this one covers the upstream skill/contract side you don't otherwise see.
> **Canonical references (not duplicated here -- go to the source, don't trust a stale copy):**
> `Docs/CENTAUR_SCHEMA_v2.json` (your own copy is authoritative) and the hub's own
> `OPTIONS_SIEVE_SPEC.md` if you need to understand *why* a payload looks the way it does.

---

## 1. Current skill versions (the things that produce your CENTAUR JSON)
- `skill-options-ibkr-radar.md`: Options IQ — IBKR Radar v2.3
- `skill-options-scanner.md`: Options IQ — Autonomous Scanner (v2.3 — Curated Edge Monitor)
- `skill-options-directional-builder.md`: Directional Trade Builder — v1.6
- `skill-options-trade-validator.md`: Options Trade Validator v3.1

---

## 2. Latest hub session (may or may not be Gemini-relevant -- read for context, not everything here concerns you)
### July 15, 2026 — Session 26
**The `get_option_data` blocker resolved itself; the actual first live forward-test journal entries went in; watchlist expanded with 20 names from Bala's conviction research.**
- **Retried `get_option_data` first thing, per the Session 25 pre-flight queue:** 4/4 clean (AFRM, GDX, OKLO, NIO chains all resolved on the first attempt) — the 15/15 failure from Jul 13 is gone. No code change on this side; root cause stays unconfirmed (most likely transient, server-side, per Session 25's isolation), but the tool is verified working again.
- **Ran the reject arm's Directional Builder pass** (OKLO, NIO) — both score BEARISH (4/5 signals: SMA200 downtrend, negative YTD, bearish EMA stack, lower-third 52w range all outweigh a single bullish avg-P/C-ratio signal), so both take the nearest-ATM put per the protocol's symmetry rule. Computed via a Python script running the skill's own Step 4/5 formulas exactly, not a different methodology — just precise arithmetic over ~250 daily bars instead of hand-eyeballing it (a question the user asked directly this session — confirmed this is not a Centaur-Mode substitute; the forward test deliberately bypasses Gemini's Centaur synthesis by design, per `FORWARD_TEST_PROTOCOL.md`, so both arms get identical mechanical contract construction).
- **Real finding surfaced re-pulling live data 2 days after Session 25's scan:** GDX's IVR drifted from 44.4% (Jul 13, passed Sieve 1 provisionally) to 48.2% (Jul 15) — it would now fail the gate it was originally selected under. Asked Bala rather than deciding unilaterally; kept as originally selected (the scan is the selection event), with the drift recorded honestly in both the journal's `setup_context` and `forward_test_log.csv` rather than silently smoothed over.
- **First live entries actually logged to Gemini's journal:** AFRM 85 Call (id 8, entry mid $5.85), GDX 74 Put (id 9, entry mid $2.835 — flagged unusually wide spread at market open), OKLO 46 Put (id 10, entry mid $3.80), all Aug 7 2026 (25 DTE). **NIO's 5 Put got `NO_QUOTE`** — two consecutive live-quote pulls at market open both returned empty bid/ask; recorded honestly per the quote-or-skip rule, not fabricated or forced.
- **Ran a full second Scanner cycle same day** (the actual Jul 15 scan, not just executing Jul 13's backlog): CORE 0/20 again (all failed Sieve 1, despite VIX calm at 16.1); EXTENDED 58/59 resolved (ABB unresolvable again, same as Session 25) with only 4 names clearing Sieve 1 (CCJ, MP, URA, XLF) and just 1 true finalist — **CCJ** (IVR 43.0%, IV/HV 97.9%). MP/URA/XLF all failed exactly Sieve 2b, becoming the reject-arm control group.
- **`get_option_data` regressed mid-session** — 4/4 clean at ~09:15 ET, then 5/5 failed at ~14:30 ET on the same session, with `get_price_snapshot` succeeding immediately after on the identical contract (rules out a broader outage). Bala's diagnosis was two-part: (1) suspected the burst of MCP calls this session (VIX + 20 CORE + 58 EXTENDED snapshots) was overloading it, and (2) asked directly why Tradier wasn't being used instead. Point (2) led somewhere real: Gemini's own `app.py` already has a working `/markets/options/chains` Tradier integration, and the token (confirmed alive since Session 24's `/tradier/ping` success) works fine for chains/quotes — the Fundamentals Beta gap only ever blocked the earnings *calendar*, not chain data. Read the token from `options_iq_gemini/.env` (with Bala's explicit go-ahead first) and called Tradier's REST API directly via curl — clean chain data for all four names on the first try. **Tradier is now the primary chain-data path for the forward test, IBKR MCP demoted to fallback** — both `FORWARD_TEST_PROTOCOL.md` and this file updated.
- **Logged the second round:** CCJ 91 Put (id 11, survivor, BEARISH 5/5 signals despite being the day's one clean pass — earnings Jul 30 is 15 days out, WITHIN HOLD not TBLA), MP 50 Put (id 12), URA 41 Put (id 13), XLF 57 Call (id 14, the day's only BULLISH name). All via Tradier, all Aug 7 2026.
- **Watchlist expansion:** Bala supplied five separate conviction-stock research tables (AI Supply Chain framework, STRATUM critical-materials framework, Canadian conviction list, US conviction list). Deduped the full union against the existing CORE/EXTENDED watchlist and against each other, then filtered out 10 Canadian TSX/Venture-only tickers with no US options market (architecture constraint, not a judgment call — Gemini trades via Tradier, US-only). Verified 8 ambiguous tickers live via `search_contracts` before proposing them (GIB, TRP, ASTS, FN, MOD, CGNX, LSCC, PATH all confirmed real US-optionable listings). Added 20 net-new tickers to `skill-options-scanner.md` EXTENDED (v2.2 → v2.3) across 5 new theme sections plus 6 existing ones; 6 of the new names (MOD, FN, CGNX, LSCC, ASTS, PATH) join the existing thin-name OI-verification list. `SKILL_MAP.md` updated to match.
- **Built `skill-session-start.md` and `skill-session-close.md` as new hub-level process skills** (Bala's call — "it doesn't hurt" to have both, even though session-start alone is thin) — formalizing the 26-session-old close ritual (Known Issues, Session History, Next Steps, SKILL_MAP.md sync, handoff regen, header rewrite, commit) and a lightweight read-only session-open orientation (cross-repo check, wall-clock/market-status anchor, short "where we are" summary). Registered in `SKILL_MAP.md` (now 8 skills) and this file's skill table. Dogfooded both same session: `session-close` correctly identified a clean working tree as a legitimate no-op rather than fabricating a Session History entry; `session-start` gave a real orientation before the next block of work.
- **Ran PATH A (manual Radar paste) for the first time this session** — Bala pasted a live 15-row IBKR scanner table. All 14 real tickers passed Sieve 1 (pre-filtered at scanner level); Gate A eliminated HIVE ($843.6M) and RILY ($290.2M) — a **third independent confirmation** of the HIVE sub-$1B finding first surfaced Sessions 24/26, this time from a completely different data path (manual paste, not a live MCP pull). Sieve 2b finalists: **AVAV, WOLF, ECHO**. WOLF's price history is only 199 bars (post-restructuring/reorg ~Sept 2025) — correctly flagged `INSUFFICIENT_HISTORY` for SMA200 rather than trusting a web search that returned three contradictory stale SMA values.
- **Directional Builder on all three: unanimous BEARISH.** Surfaced a second real, gate-flipping finding: AVAV's and WOLF's pasted watchlist IVR (40, 26 — both pass) diverged sharply from MCP's own percentile computation (65.3%, 58.7% — both would fail) — a bigger, gate-flipping version of the AFRM divergence documented since Session 19. Paste-Rank treated as authoritative per Radar's own rule; both flagged prominently, not silently smoothed over.
- **First real end-to-end Centaur round-trip this session — found a genuine, previously undiscovered contract bug.** POSTing AVAV's real payload to `/analyze/centaur` triggered `EDGE_VIOLATION` on a 71.4% IV/HV — a genuinely good edge, not a violation. Root-caused to `app.py:762`: `iv_hv_ratio >= 1.0` expects a decimal fraction (confirmed via `test_centaur_contract.py`'s own fixtures, `0.67`/`1.35`), but the Directional Builder skill's documented formula and the CENTAUR_SCHEMA field both produce a percentage-style number — every real submission following the skill's own convention would always false-trigger this gate. Bala's call: unblock today's calls by sending `iv_hv_ratio` as a fraction (÷100), log it as a HIGH/cross-repo Known Issues finding, and draft `HANDOFF_gemini_iv_hv_units.md` for Gemini's dev session to fix the root cause properly (their call: fix the skill's units or fix their own `>= 100` threshold). AVAV, WOLF, and ECHO all then correctly reached chain resolution and stood down for a real, separate reason (no contract cleared OI/spread/delta/Liquidity Gravity across 67–86 contracts checked each) — a legitimate result, not another instance of the bug.
- **Logged all three to the forward test anyway** (AVAV 140 Put id 15, WOLF 35 Put id 16, ECHO 93 Put id 17) — per protocol, Gemini's synthesis (or stand-down) is metadata only and never drives forward-test logging, which stays mechanical. First PATH A entries in the forward test, flagged explicitly in the CSV as a new dimension alongside the existing PATH B entries.
- **Closed out two more loose ends:** retried NIO's quote via Tradier (the morning's IBKR `NO_QUOTE` was a real IBKR-side gap, not genuine illiquidity — Tradier returned a clean quote with OI 765) and logged it (id 18). Logged INFQ, today's Radar scan's own near-miss reject (IV/HV exactly 100.0%) — which surfaced a **new edge case in the DTE rule**: no expiry at all fell inside 21–35 DTE for this name (only a 2-day or a 37-day option existed, no weekly in between). Asked Bala rather than deciding unilaterally; used the 37-day expiry as the closer overshoot, and wrote this exception into `FORWARD_TEST_PROTOCOL.md`'s contract-construction rule directly (it had never actually been updated past "nearest monthly," despite the Session 25 weekly-fallback rule having been decided in prose only). **12 total forward-test positions now OPEN.**

---

## 3. Open cross-repo Known Issues (from the hub's Known Issues table, filtered to rows tagged "cross-repo")
| HIGH (finding, cross-repo, unblocked not fixed) | `iv_hv_ratio` units mismatch silently breaks every real Centaur submission | Session 26 (Jul 15, 2026): first real end-to-end round-trip of a live Directional Builder payload through `/analyze/centaur` (previous testing either used mocked payloads or stood down before reaching this gate). `options_iq_gemini/app.py:762` checks `float(iv_hv_ratio) >= 1.0`, confirmed via its own `test_centaur_contract.py` fixtures (`0.67` passes as DEEP_BUYER_EDGE, `1.35` triggers stand-down) to expect a **decimal fraction**. But `skill-options-directional-builder.md`'s own documented formula and the CENTAUR_SCHEMA_v2 field both produce **percentage-style numbers** (e.g. 71.40 for "71.40%"), matching how IVR is handled elsewhere in the same file (`iv_rank >= 45`, also percentage-style, line 605) and the skill's own display table (`<70% / 70–100% / 100–115% / >115%`). Net effect: any real payload following the skill's documented convention has an `iv_hv_ratio` value that is always ≥ 1.0 in raw form, so it **always** false-triggers `EDGE_VIOLATION` regardless of the real edge — this would have silently killed AVAV (71.4%, genuinely BUYER_EDGE) at the gate before it ever reached chain resolution. **Unblocked for today's live calls** (sent `iv_hv_ratio` as a fraction, ÷100, per Bala's explicit call) — AVAV/WOLF/ECHO then correctly reached chain resolution and stood down for a real reason (no contract cleared OI/spread/delta/Liquidity Gravity). **Root cause not fixed** — needs a HANDOFF to Gemini's dev session to decide whether the skill changes its convention (divide by 100 before emitting) or Gemini's check changes to `>= 100` (more consistent with IVR's own percentage-style handling in the same file). See `HANDOFF_gemini_iv_hv_units.md`. |
