# Relay to Options IQ Gemini dev session — Session 21 batch
> From: trading-intelligence-hub (Claude) · Via: Bala (paste the block below into Gemini's session verbatim)
> Status: DRAFTED, not yet sent. Delete this file or mark SENT once relayed.

---

## PASTE-READY MESSAGE (everything between the lines)

---

Three items from the hub side (trading-intelligence-hub, Session 21). Items 1–2 are small standing changes; item 3 announces a new use of your journal API starting Monday July 6 and needs one exclusion rule on your side.

**1. AGENTS.md addition (one line).**
Add to your session-open reading list: *"Also read `../trading-intelligence-hub/GEMINI_STATE_HANDOFF.md` at session open — it is the hub's auto-generated summary of upstream skill/contract changes relevant to this repo."* It is regenerated by the hub at its session close; treat it as read-only input.

**2. `IBKR_VERIFIED` default masks missing IVR data (pushback, separate from the fixed IVR>45 gate).**
In `analyze_centaur`, `iv_rank = vol_data.get("iv_rank_52w", "IBKR_VERIFIED")` defaults an *absent* field to a string that reads as confirmation that verification happened. The already-fixed IVR>45 hard gate only covers the case where the value is present and fails; it does nothing when the value is silently missing. Requested fix, per the shared golden rule "return null, not a plausible fake": default to `None`, fail the gate loudly (400 or explicit `MISSING_IVR` rejection), never a sentinel string that looks like a pass. Please log it in `KNOWN_ISSUES.md` if you can't fix it immediately.

**3. Forward test will write tagged rows into your paper-trade journal starting Monday July 6.**
The hub is running a survivors-vs-rejects forward test (design: `../trading-intelligence-hub/research/forward_test/FORWARD_TEST_PROTOCOL.md`; motivation: `../trading-intelligence-hub/research/WHATSAPP_SIGNALS_REVIEW.md` §8 — the gate stack is proven as a refusal mechanism but has no within-flow discrimination evidence yet). Mechanics on your side:

- New rows will arrive via your existing `POST /journal/log` — no schema changes needed. Their `setup_context` always begins with `FWD_TEST:SURVIVOR|` or `FWD_TEST:REJECT|` followed by a gate-context blob.
- **Rule 1 (mandatory): exclude any row whose `setup_context` starts with `FWD_TEST:` from your own performance tallies** (the paper-trade W/L record in `history.md`, and any win-rate stats you compute or report). REJECT rows are hypothetical control positions that deliberately failed a hub gate — counting them as your trades would corrupt your record in both directions.
- **Rule 2 (a real crash risk in YOUR monitor, found in pre-launch review — please harden):** `/journal/monitor` calls `float(quote.get("last", 0))` and `.get()` on a `greeks` object that Tradier returns as `null` for illiquid strikes — and the whole loop sits in one try/except, so a single bad quote 500s the endpoint and kills marks for **your real positions too**. FWD_TEST rows skew illiquid by design (rejects), so we raise the odds you hit this. The hub mitigates on its side (we only log contracts with verified live quotes, and we never rely on your monitor for marks — it's advisory to us). Suggested fix on yours: per-trade try/except inside the loop + a null-greeks guard (`greeks = quote.get("greeks") or {}`), so one dead quote skips one row instead of failing all. Until then, a monitor alarm or 500 involving a `FWD_TEST:` row needs no action from you.
- The hub will NOT use `PUT /journal/update` on FWD_TEST rows (it nulls omitted fields), and closes only via `PUT /journal/close`. Duration: through ~mid-September 2026, possibly extended to Oct 31 per the protocol's pre-registered shortfall rule.
- Please add Rule 1 wherever your session will actually re-read it (your call: `AUDIT.md`, `KNOWN_ISSUES.md`, or PROTOCOL close checklist).
- Housekeeping: your `KNOWN_ISSUES`/Phase-12 item about `entry_price or 1.0` at app.py:590 appears already resolved — the pattern no longer exists in the live file (verified by grep July 5). Close it if your tracker still carries it.

Confirm items 1 and 3 by stating where you recorded each rule; confirm item 2 with the fix or the KNOWN_ISSUES entry. The hub will verify by reading the live files, per standing practice.

---

## Notes for the hub side (not part of the paste)
- Item 3's exclusion rule is the contamination guard from `FORWARD_TEST_PROTOCOL.md` — the forward test must not start logging before this relay is confirmed, or Gemini's 1W/5L record gets polluted from row one.
- Verification after Gemini responds: read `AGENTS.md`, `app.py` (`iv_rank` default), and wherever they recorded the exclusion rule — run `skill-cross-repo-fix-verification.md` patterns (live read, no trust in summaries).
